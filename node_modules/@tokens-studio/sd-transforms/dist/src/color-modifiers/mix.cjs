'use strict';

var Color = require('colorjs.io');

function _interopDefault (e) { return e && e.__esModule ? e : { default: e }; }

var Color__default = /*#__PURE__*/_interopDefault(Color);

function mix(color, colorSpace, amount, mixColor) {
    const mixValue = Math.max(0, Math.min(1, Number(amount)));
    return new Color__default.default(color.mix(mixColor, mixValue, { space: colorSpace }).toString());
}

exports.mix = mix;
