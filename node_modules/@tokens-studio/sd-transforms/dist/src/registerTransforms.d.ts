import type StyleDictionary from 'style-dictionary';
import { TransformOptions } from './TransformOptions.js';
export declare const transforms: string[];
/**
 * typecasting since this will need to work in browser environment, so we cannot
 * import style-dictionary as it depends on nodejs env
 */
export declare function registerTransforms(sd: typeof StyleDictionary, transformOpts?: TransformOptions): Promise<void>;
